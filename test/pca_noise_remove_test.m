function pca_noise_remove_test(data,sample_artifact_pos,sample_before,sample_after)
%data: column-wise multi-channel bio-signal
%sample_artifact_pos: a vector that define the sample position of artifacts
%sample_before: sample number before the label position
%sample_after: sample number after the label position

for i=1:length(sample_artifact_pos)
    
end

end

